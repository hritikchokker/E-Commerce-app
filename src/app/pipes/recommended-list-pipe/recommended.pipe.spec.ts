import { RecommendedPipe } from './recommended.pipe';

describe('RecommendedPipe', () => {
  it('create an instance', () => {
    const pipe = new RecommendedPipe();
    expect(pipe).toBeTruthy();
  });
});
