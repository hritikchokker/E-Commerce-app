import { SelectpipePipe } from './selectpipe.pipe';

describe('SelectpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectpipePipe();
    expect(pipe).toBeTruthy();
  });
});
